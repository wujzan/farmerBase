<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <!-- bootstrap 第5版不支援IE10 IE11 -->
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"> -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@4.0/dist/fancybox.css">
    <link rel="stylesheet" href="/css/style.css">

    <link rel="icon" href="/images/favicon.svg" />
    <script src="https://code.jquery.com/jquery-1.12.4.min.js"></script>
</head>

<body>
    <div class="ajaxbg">
        <div class="ajaxinner">
            <h3>正再跳轉到...</h3>
            <img src="/images/ajax-loader.gif" alt="">
        </div>
    </div>

    <!-- navbar-頂置顯示 -->
    <nav class="navbar navbar-default">
        <!-- <div class="container"> -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                
                <a class="navbar-brand navlogo" href="/"><img src="/images/logo.svg" alt="Logo"  width="20" height="20"/>小農基地Farmer's Base</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <!-- <ul class="nav navbar-nav">
                    <% pages.forEach(function(page){ %>
                        <% if(page.slug=="home" ){ %>
                            <li><a href="/"><%= page.title %></a></li>
                        <% }else{ %>
                            <li><a href="/<%= page.slug %>"><%= page.title %></a></li>
                        <% } %>
                    <% }); %>
                </ul> -->

                <ul class="nav navbar-nav navbar-right">                    
                    <% if(user){ %>
                        <li>
                            <a href="/favorite/bookmarks">我的最愛 ❤</a>
                        </li>
                        
                        <li><a href="/users/edit-profile">Hi, <%= user.username %></a></li>
                        <li><a href="/users/logout">登出</a></li>
                    <% }else{ %>
                        <li><a href="/users/register">註冊</a></li>
                        <li><a href="/users/login">登入</a></li>
                    <% } %>

                    <% if(user && user.admin == 1){ %>
                        <li><a href="/admin/members">Admin後台管理</a></li>
                    <% } %>

                </ul>
            </div>        
    </nav>

    <div class="bar">
        <div class="logo">
            <img src="/images/logo.svg" alt="Logo" />
            <a href="/"><h1>小農基地Farmer's Base</h1></a>
        </div>

        <div class="category">
            <ul>
                <li><a href="/articles">全部資訊</a></li>

                <% categories.forEach(function(c){ %>
                    <li><a href="/articles/<%= c.slug %>"><%= c.title %></a></li>
                <% }); %>
            </ul>
        </div>
    </div>    

    <div class="container">
        <%- messages('messages', locals) %>

        <% if(errors) { %>
            <% errors.forEach(function(error) { %>
            <div class="alert alert-danger">
              <%= error.msg %>
            </div>
            <% }); %>
        <% } %>

        <div class="row">
            <!-- <div class="col-xs-12 col-md-3">
                <h3>分類</h3>
                <ul class="list-group">
                    <li class="list-group-item"><a href="/articles">全部資訊</a></li>
                    <% categories.forEach(function(c){ %>
                        <li class="list-group-item"><a href="/articles/<%= c.slug %>"><%= c.title %></a></li>
                    <% }); %>
                </ul>
            </div> -->

            <!-- <div class="col-xs-12 col-md-1"></div> -->

            <div class="col-xs-12 col-md-9 col-md-offset-1">